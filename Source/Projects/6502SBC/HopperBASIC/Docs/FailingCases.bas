CLS
NEW
CONST WORD sizepl = 8191
BIT flags[sizepl]

BEGIN
    WORD K
    WORD I
    IF flags[i] THEN
        WHILE k < sizepl
            PRINT
        WEND
        count = count + 1
    ENDIF

END

RUN
